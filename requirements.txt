# Core scientific stack
numpy>=1.24
scipy>=1.11
matplotlib>=3.7

# Geospatial
geopandas>=0.14
shapely>=2.0
rasterio>=1.3
laspy[lazrs]>=2.5
gpxpy>=1.6

# 3D
open3d>=0.18

# Image processing
Pillow>=10.0

# NOTE: pdal nie jest na pip dla wszystkich platform.
# Instaluj przez conda-forge (patrz environment.yml):
#   conda install -c conda-forge pdal python-pdal
